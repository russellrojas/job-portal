<!DOCTYPE html>
<html >
    <head>
        <title>Ecran Recruteur - Page Principale</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <link rel="stylesheet" href="/stylesheets/styles.css">

        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        <%- include('header') %>
        <main style="margin-top: 15px; padding-top: 0;">
            <div align="right" >    
                <form action="/candidat/demander_devenir_admin" style="display: inline-block; margin-right: 10px;" method="GET">
                    <button type="submit" class="btn btn-sm btn-outline-secondary" name="dev_adm">Devenir admin</button>
                </form>
            </div>
            <h2><strong>Espace récruteur</strong></h2>    
            <div class="container bg-light p-3 border">
                <p><strong>Organisation : </strong><span><%= organisation.nom %></span></p>
                <p><strong>Description :</strong> <span><%= organisation.types %></span></p>
                <p><strong>Localisation :</strong> <span><%= organisation.siege_social %></span></p>
            </div> 
            <div align="right" style="margin-top: 10px;">
                <form action="/recruteur/rejoindre_creer_une_organisation" method="GET" style="display: inline-block; margin-right: 10px;">
                    <button type="submit" class="btn btn-success">Rejoindre une autre organisation</button>
                </form>
            </div>

            <h3>Offres en cours:</h3>
            <%- include('filter', { filtres: filtres, filtres_customs: null, filtres_checkbox: null, numero: 1}) %>
            <table class="table table-striped mt-3">
                <thead>
                    <tr>
                        <th>Intitulé</th>
                        <th>Responsable hiérarchique</th>
                        <th>Date de validité</th>
                        <th>Indication</th>
                        <th>Voir candidatures</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (offres && offres.length > 0) { %>
                        <% offres.forEach(function(offre) { %>
                            <tr id="tr1">
                                <td><%= offre.intitule %></td>
                                <td><%= offre.responsable_hierarchique %></td>
                                <td><%= offre.date_validite.toISOString().split('T')[0] %></td>
                                <td><%= offre.indication %></td>
                                <td>
                                    <form action="/recruteur/gestion_des_candidatures" method="POST">
                                        <input type="hidden" name="offreId" value="<%= offre.id %>">
                                        <button type="submit" class="btn btn-danger">Candidats</button>
                                    </form>                                    
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="5" class="text-center">Aucune offre d'emploi disponible.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
            <%- include('paginator', {taille: offres.length , numero: 1})%>
            <form action="/candidat/ecran_candidat" method="get">
                <button type="submit" class="btn btn-primary btn-sm">Retourner</button>
            </form>            
        </main>
    </body>
</html>
