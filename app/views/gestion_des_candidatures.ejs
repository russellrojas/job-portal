<!DOCTYPE html>
<html >
    <head>
        <title>Gestion des candidatures</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <link rel="stylesheet" href="/stylesheets/styles.css"> 
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        <%- include('header')%> 
        <main style="margin-top: 15px; padding-top: 0;">
            <div align="right" >    
                <form action="/candidat/demander_devenir_admin" style="display: inline-block; margin-right: 10px;" method="GET">
                    <button type="submit" class="btn btn-sm btn-outline-secondary" name="dev_adm">Devenir admin</button>
                </form>
            </div>
            <h2><strong>Gestion des candidatures</strong></h2>  
            <h4><strong>Poste:</strong> <%= offre.intitule_poste %></h4>
            
            <div class="container bg-light p-3 border">
                <div class="row">
                    <div class="col-md-6">
                        <p><strong>Organisation:</strong> <span><%= organisation.nom %></span></p>
                        <p><strong>Indication:</strong> <span><%= offre.indication || 'Non disponible' %></span></p>
                        <p><strong>Statut de l'offre:</strong> <span><%= offre.statut_offre || 'Non disponible' %></span></p>
                    </div>
                    <div class="col-md-6">
                        <p><strong>Responsable hiérarchique:</strong> <span><%= offre.responsable_hierarchique || 'Non disponible' %></span></p>
                        <p><strong>Date de validité:</strong> <span><%= offre.date_validite || 'Non disponible' %></span></p>
                    </div>
                    <p><strong>Description:</strong> <span><%= offre.description || 'Non disponible' %></span></p>
                </div>
            </div>
            <!-- Filtres -->
            <h4><strong>Liste des Candidats:</strong></h4>
            <%- include('filter', { filtres: filtres, filtres_customs: null, filtres_checkbox: null, numero: 1}) %>
            <div class="container">
                <% if (!candidats || candidats.length === 0) { %>
                    <div class="alert alert-info mt-3" role="alert">
                        Aucun candidat pour cette offre.
                    </div>
                <% } else { %>
                    <table class="table table-striped mt-3">
                        <thead>
                            <tr>
                                <th>Nom</th>
                                <th>Prénom</th>
                                <th>Date de Candidature</th>
                                <th>Pièces jointes (nb)</th>  
                                <th>Dossier</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% candidats.forEach(function(candidat) { %>
                                <tr id="tr1">
                                    <td><%= candidat.nom %></td>
                                    <td><%= candidat.prenom %></td>
                                    <td><%= candidat.date_candidature %></td>
                                    <td>
                                        <% if (candidat.nb_documents > 0) { %>
                                            Oui (<%= candidat.nb_documents %>)
                                        <% } else { %>
                                            Non (0)
                                        <% } %>
                                    </td>
                                    <td>
                                        <form action="/gestion_des_fichiers/telecharger_dossier" method="POST">
                                            <input type="hidden" name="offreId" value="<%= offre.id %>">
                                            <input type="hidden" name="candidatId" value="<%= candidat.id_candidat %>">
                                            <button type="submit" 
                                                    class="btn btn-danger"
                                                    <% if (candidat.nb_documents <= 0) { %> disabled <% } %>>Télécharger
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                    <%- include('paginator', {taille: candidats.length , numero: 1})%>
                <% } %>
            </div>
            <form action="/recruteur/ecran_recruteur" method="GET">
                <button type="submit" class="btn btn-primary">Retourner</button>
            </form><br>

        </main>
    </body>
</html>
