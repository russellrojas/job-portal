<!DOCTYPE html>
<html >
    <head>
        <title>Ecran demander devenir recruteur</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <link rel="stylesheet" href="/stylesheets/styles.css">
        
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        <%- include('header')%> 
        <main style="margin-top: 15px; padding-top: 0;">
            <div align="right">    
                <form action="/candidat/demander_devenir_admin" style="display: inline-block; margin-right: 10px;" method="GET">
                    <button type="submit" class="btn btn-sm btn-outline-secondary" name="dev_adm">Devenir admin</button>
                </form>    
            </div>
            <h2 class="mb-4"><strong>Détails de mon compte </strong></h2>
            <div class="container bg-light p-2 border" >
                <div class="row">
                <div class="col-md-6 mb-2">
                    <p><strong>Nom complet : </strong> <%= utilisateur.nom + " " + utilisateur.prenom %></p>
                    <p><strong>Téléphone : </strong> <%= utilisateur.telephone %></p>
                    <p><strong>Email : </strong> <%= utilisateur.email %></p>
                </div>
                <div class="col-md-6 mb-2">
                    <p><strong>Type de compte : </strong> "<%= utilisateur.type_utilisateur %>"</p>
                    <p><strong>Organisation : </strong> <%= organisation ? organisation.nom  : "Vous n'appartenez à aucune organisation." %></p>
                    <p><strong>Date de création : </strong><%= utilisateur.date_creation.toLocaleDateString('fr-FR') %> </p>
                </div>
                </div>
            </div></br>
            <h4><strong>Etat de la demande :</strong></h4> 
            <div align="center">
                <h4><%= etatDemandeRecruteur ? etatDemandeRecruteur : "Pas encore de demande" %></h4>
                <% if (!etatDemandeRecruteur || etatDemandeRecruteur === 'refusée') { %>
                    <form action="/candidat/demander_devenir_recruteur" method="post" style="display: inline-block; margin-right: 10px;">
                        <button type="submit" class="btn btn-success" name="dev_adm">(Re) faire une demande</button>
                    </form>
                <% } %>
                <% if (etatDemandeRecruteur && etatDemandeRecruteur !== 'refusée') { %>
                    <form action="/candidat/annuler_demander_devenir_recruteur" method="post" style="display: inline-block; margin-right: 10px;">
                        <button type="submit" class="btn btn-danger">Annuler demande</button>
                    </form>
                <% } %>
                <form action="/candidat/ecran_candidat" method="get" style="display: inline-block; margin-right: 10px;">
                    <button type="submit" class="btn btn-primary" name="ret">Retourner</button>
                </form>
            </div>
        </main>
    </body>
</html>
