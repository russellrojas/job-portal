<!DOCTYPE html>
<html lang="en">
<head>
  <title>Gestion des utilisateurs</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="/stylesheets/styles.css"> 
</head> 
<body>

<%- include('header')%>

<div class="d-flex">
    <nav class="nav nav-expand-lg bg-body-tertiary flex-column justify-content-center">
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link" href="/admin/gestion_des_admins">Demandes admin</a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="/admin/gestion_des_recruteurs">Demandes recruteurs</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/admin/gestion_des_organisations">Demandes organisations</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/admin/gestion_des_adhesions_aux_organisations">Demandes adhésions</a>
            </li>
          </ul>
    </nav>

    <div class="d-flex flex-column bd-highlight ms-5 mt-5 gap-4">
        <div class="flex me-15 fs-5 gap-20">
            <p>Liste des utilisateurs :</p>  
        </div>
       
        <%- include('filter', { filtres: filtres, filtres_customs: null, filtres_checkbox: filtres_checkbox, numero: 1}) %>
        
        <table class="table table-striped">
            <thead>
              <tr>
                <th scope="col">Nom</th>
                <th scope="col">Prénom</th>
                <th scope="col">Email</th>
                <th scope="col">n° télephone</th>
                <th scope="col">Type de l'utilisateur</th>
                <th scope="col">Modifier l'utilisateur</th>
              </tr>
            </thead>
              <tbody>
                <% users.forEach((user)=> { %>
                  <tr id="tr1">
                    <td>
                      <% if(user.actif == true){ %>
                        <%= user.nom %> 
                      <% }else{ %>
                        <strike> <%= user.nom %></strike>
                      <% } %>
                    </td>
                    <td>
                      <% if(user.actif == true){ %>
                        <%= user.prenom %>
                      <% }else{ %>
                        <strike> <%= user.prenom %></strike>
                      <% } %>
                    </td>
                    <td>
                      <% if(user.actif == true){ %>
                        <%= user.email %>
                      <% }else{ %>
                        <strike> <%= user.email %></strike>
                      <% } %>
                    </td>
                    <td>
                      <% if(user.actif == true){ %>
                        <%= user.telephone %>
                      <% }else{ %>
                        <strike> <%= user.telephone %></strike>
                      <% } %>
                    </td>
                    <td>
                      <% if(user.actif == true){ %>
                        <%= user.type_utilisateur %>
                      <% }else{ %>
                        <strike> <%= user.type_utilisateur %></strike>
                      <% } %>
                    </td>
                    <td>
                      <form action="/admin/modification_d_un_utilisateur/" method="POST">
                        <input type="hidden" name="id" value="<%= user.id %>">
                        <input type="hidden" name="nom" value="<%= user.nom %>">
                        <input type="hidden" name="prenom" value="<%= user.prenom %>">
                        <input type="hidden" name="telephone" value="<%= user.telephone %>">
                        <input type="hidden" name="actif" value="<%= user.actif %>">
                        <input type="hidden" name="siren_organisation" value="<%= user.siren_organisation %>">
                        <input type="hidden" name="email" value="<%= user.email %>">
                        <input type="hidden" name="type_utilisateur" value="<%= user.type_utilisateur %>">
                        <button class="btn btn-outline-secondary ms-2" type="submit" style="border: none;">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-pen" viewBox="0 0 16 16">
                                <path d="m13.498.795.149-.149a1.207 1.207 0 1 1 1.707 1.708l-.149.148a1.5 1.5 0 0 1-.059 2.059L4.854 14.854a.5.5 0 0 1-.233.131l-4 1a.5.5 0 0 1-.606-.606l1-4a.5.5 0 0 1 .131-.232l9.642-9.642a.5.5 0 0 0-.642.056L6.854 4.854a.5.5 0 1 1-.708-.708L9.44.854A1.5 1.5 0 0 1 11.5.796a1.5 1.5 0 0 1 1.998-.001m-.644.766a.5.5 0 0 0-.707 0L1.95 11.756l-.764 3.057 3.057-.764L14.44 3.854a.5.5 0 0 0 0-.708z"/>
                              </svg>
                        </button>
                      </form>
                    </td>
                  </tr>
                <% }) %>
            </tbody>
          </table>
          <%- include('paginator', {taille: users.length , numero: 1}) %>
        </div>
    </div>
<footer></footer>
</body>
</html>