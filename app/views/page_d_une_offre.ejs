<!DOCTYPE html>
<html >
    <head>
        <title>Page d'une offre</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <link rel="stylesheet" href="/stylesheets/styles.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        <%- include('header')%> 
        <main style="margin-top: 15px; padding-top: 0;">
            <div align="right" >    
                <form action="/candidat/demander_devenir_admin" style="display: inline-block; margin-right: 10px;" method="GET">
                    <button type="submit" class="btn btn-sm btn-outline-secondary" name="dev_adm">Devenir admin</button>
                </form>
                <% if (session.type_utilisateur==='candidat') { %>
                    <form action="/candidat/demander_devenir_recruteur" style="display: inline-block;" method="GET">
                        <button type="submit" class="btn btn-sm btn-outline-secondary" name="dev_rec">Devenir recruteur</button>
                    </form>
                <% } %>
            </div>

            <% if (!offre) { %>
                <div class="alert alert-warning" role="alert">
                    Il n'y a pas d'offre disponible pour cet identifiant.
                </div>
            <% } else { %>
                <h2 style="margin: 15px;"><strong>Offre d'emploi:</strong> <%= offre.intitule_poste %></h2>
                <div class="d-flex justify-content-center gap-3 mt-3"> 
                    <% if (offresCandidatures.includes(offre.id)) { %>
                        <form action="./page_d_une_candidature" method="POST" >
                            <input type="hidden" name="offreEmploiId" value="<%= offre.id %>">
                            <button type="submit" class="btn btn-success btn">Gérer Candidature</button>
                        </form>                       
                        <form action="/candidat/annuler_candidature" method="POST" >
                            <input type="hidden" name="offreEmploiId" value="<%= offre.id %>">
                            <button type="submit" class="btn btn-danger">Annuler candidature</button>
                        </form>
                    <% } else { %>
                        <form action="/candidat/candidater" method="POST" >
                            <input type="hidden" name="offreEmploiId" value="<%= offre.id %>">
                             <input type="hidden" name="candidatureRecente" value="true">
                            <button type="submit" class="btn btn-success">Candidater</button>
                        </form>
                    <% } %>
                </div>

                <div class="container bg-light p-3 border" style="margin-top: 15px; " >
                    <p><strong>Entreprise:</strong> <span><%= offre.organisation_nom %></span></p>
                    <p><strong>Type de contrat:</strong> <span><%= offre.statut_poste %></span></p>
                    <p><strong>Type de métier:</strong> <span><%= offre.type_metier %></span></p>
                    <p><strong>Lieu de la mission:</strong> <span><%= offre.lieu_mission %></span></p>
                    <p><strong>Temps de travail:</strong> <span><%= offre.rythme %></span></p>
                    <p><strong>Salaire:</strong> <span><%= offre.salaire_min && offre.salaire_max ? offre.salaire_min + ' - ' + offre.salaire_max : '' %></span></p>
                    <p><strong>Responsable hiérarchique:</strong> <span><%= offre.responsable_hierarchique %></span></p>
                    <p><strong>Indication:</strong> <span><%= offre.indication %></span></p>
                    <p><strong>Description:</strong> <span><%= offre.description %></span></p>
                </div>
            <% } %>
            </br>
            <div align="center">
                <form action="/candidat/ecran_candidat" method="GET" style="display: inline-block;">
                    <button type="submit" class="btn btn-primary">Retourner voir les offres</button>
                </form>
            </div>
        </main>
    </body>
    
</html>
